- @page_title = 'Editar Pedido #' + @order.number

= content_for :header do
  %p.buttons
    = link_to "Ir a Pedidos", @order, :class => 'button grey'
  %h2.orders Editar Pedido <b>##{@order.number}</b>

= form_for @order, :html => {:class => 'orderForm'} do |f|
  = f.error_messages
  = render 'form', :f => f
  

  = field_set_tag "Pedido", :class => 'padded' do
    = render 'order_items_form', :f => f

  %p.submit
    = f.submit "Guardar Pedido", :class => 'button green'
    = link_to "Cancelar", @order, :class => 'button'